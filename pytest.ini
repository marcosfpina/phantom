[pytest]
# Pytest configuration for Phantom project

# Test discovery
testpaths = tests
python_files = test_*.py *_test.py
python_classes = Test* *Tests
python_functions = test_*

# Python path
pythonpath = src

# Coverage
addopts =
    # Verbose output
    -v
    # Show local variables in tracebacks
    -l
    # Show summary of all test outcomes
    -ra
    # Strict markers
    --strict-markers
    # Strict config
    --strict-config
    # Coverage options (commented out by default)
    # --cov=phantom
    # --cov-report=term-missing
    # --cov-report=html
    # --cov-report=xml
    # Warnings (configured via filterwarnings section below)

# Asyncio mode
asyncio_mode = auto

# Markers
markers =
    unit: Unit tests (fast, isolated)
    integration: Integration tests (slower, multiple components)
    e2e: End-to-end tests (slowest, full system)
    slow: Tests that take significant time
    gpu: Tests requiring GPU
    llm: Tests requiring LLM API access

# Minimum Python version
minversion = 3.11

# Output options
console_output_style = progress
log_cli = false
log_cli_level = INFO

# Filtering
filterwarnings =
    error
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::UserWarning:sentence_transformers
    ignore::FutureWarning
    ignore::FutureWarning:transformers
    ignore:.*TRANSFORMERS_CACHE.*:FutureWarning

# Doctest
doctest_optionflags = NORMALIZE_WHITESPACE ELLIPSIS
